/** Client- and server-safe public URL helpers. For production startup checks, import `assertProductionConfiguration` from `@/lib/env/assert-production` (server-only). */
export { getMetadataBaseUrl, getPublicSiteUrl } from './public';
